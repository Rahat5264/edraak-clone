- Prefers clean, flowing content layouts (headings + paragraphs) over card-based UI components like "evs overview" cards, "what helps" cards, or image cards with decorative overlays. Confidence: 0.9
- Prefers detail page titles overlaid in white text on the featured image (full-width hero with dark overlay), centered in the image — not in a separate white section below it. E.g., blog detail pages should have the title in white centered over the feature image like "Meet us at Abu Dhabi Sustainable Week" style. Confidence: 0.8
- Values visual consistency — references existing pages on the same site as the desired format pattern rather than introducing novel layouts. Confidence: 0.8
- Prefers two-column detail page layout (text left, image right) over image-first stacked layout. Confidence: 0.9
- Prefers rich, structured page content with proper headings and detailed paragraphs over brief summary-style cards. Confidence: 0.8
- Each industry/category card should have a visually distinct, unique image — does not want duplicate images shared across different industries on listing/detail pages. Confidence: 0.8
- Wants consistent typography across pages — body text font size on detail pages must match the standard size used elsewhere on the site (e.g., no `text-sm` when other pages use `text-base` or `text-lg`). Confidence: 0.8
- Expects listing/grid cards across different sections of the site to be pixel-identical — same image height, padding, card structure, background color, and button style (e.g., Other Industries cards must exactly match Products cards in all visual dimensions). Confidence: 0.9
- Expects all site pages (including detail/child pages) to be included in the sitemap — sitemap completeness matters and missing entries should be proactively fixed. Confidence: 0.8
- Prefers dynamic/automated solutions over manual maintenance for site infrastructure — e.g., sitemap should auto-discover new static pages and fetch blog slugs from the CMS API rather than requiring manual updates when content is added. Confidence: 0.8
- Expects exhaustive cross-reference updates when renaming routes or restructuring — every file that references the old name (sitemap, content JSON, navigation config, redirects, pages, data imports) must be updated as part of the same change, not just the primary route files. Confidence: 0.8
- Does not want placeholder or filler entries (e.g., "Custom Lines" as a generic slot for future use) — only real, actual content items should exist on listing and detail pages. Confidence: 0.9
- Prefers separating content/presentation data from component code — explicitly requests extracting inline industry/page data into standalone JSON files (e.g., `data/other-industries.json`) and importing them rather than keeping large data arrays inside page components. Confidence: 0.9
- Prefers externalizing API base URLs and server endpoints into environment variables (`.env`) rather than hardcoding them in source code — e.g., splitting the server domain into a `.env` variable while using relative paths in code. Confidence: 0.8
- Prefers server-side data fetching over client-side for content pages (blog posts, listings, etc.) — explicitly requests fetching on the server side for SEO and pre-rendered content. Confidence: 0.7
- Prefers leveraging CMS metadata fields (e.g., SEOPress title, description, keywords, social images) directly from the API response for page SEO metadata, rather than manually re-entering or generating it separately. Confidence: 0.7
- Prefers 3-column grid layout for blog listing cards (on both homepage section and blog listing page) — explicitly asked to switch from 2 columns to 3 columns while keeping product-card styling. Confidence: 0.8
- Link/action colors on detail pages must match the site's primary color scheme (blue used on cards/theme), not a different color — e.g., "Back to all posts" should use blue, not green. Confidence: 0.8
- The specific brand blue is the footer's dark navy (`#020026` / `var(--site-header-bg)`), not the teal/green of `text-primary` (`#008B7D`). Section headings like "Newsletter & Blogs" and links like "View all posts" should use this dark navy footer blue, not the `text-primary` teal. Confidence: 0.9
- When adding a new site section/link (e.g., "Blog"), it must appear in **all primary navigation areas** — both the header nav and the footer nav — for consistent site-wide discoverability, not just in one place. Confidence: 0.9
- Components must faithfully render all items configured in the content/data layer — no hardcoded filters, exclusions, or transformations that silently hide entries from the rendered output. If data exists in the content JSON, it should appear in the UI unless explicitly asked otherwise. Confidence: 0.9
- Prefers SEO-optimized content — when expanding or updating page copy, content should be written with search-engine optimization in mind: keyword-rich headings, detailed explanatory paragraphs, clear structure, and thorough coverage of the topic rather than brief summaries. Confidence: 0.8
